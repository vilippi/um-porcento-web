import HeroSection from "../components/HeroSection";

const Index = () => {
    return (
        <main className="min-h-screen">
            <HeroSection />
        </main>
    );
};

export default Index;
